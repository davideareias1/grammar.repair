services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - RATE_LIMIT=${RATE_LIMIT:-6}
    env_file:
      - .env.local
